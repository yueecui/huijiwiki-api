{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "sourceMap": false /* Create source map files for emitted JavaScript files. */
    },
    "include": ["src/**/*"],
    "exclude": ["node_modules", "dist", "src/secret.ts", "src/test.ts"]
}
